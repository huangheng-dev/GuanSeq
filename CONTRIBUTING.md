# 参与 GuanSeq 贡献

感谢参与贯序。项目面向普通制造企业，优先交付可验证的业务闭环，而不是堆叠页面或技术组件。

## 开始之前

1. 阅读 `AGENTS.md`、`docs/开发规范与交付流程.md` 和与改动有关的产品、架构、ADR 文档。
2. 先搜索现有 Issue，避免重复工作。较大的领域、架构或迁移改动应先提交讨论，明确业务对象、状态、权限、数据所有权和验收条件。
3. 不要把 Mock、规划项、仿真设备或未接入能力描述为正式完成。

## 本地开发

前端要求 Node.js 24、pnpm 11；后端要求 Java 25、Docker 和 PostgreSQL 18。安装与启动方式见 `README.md`。数据库迁移以 Flyway 为事实来源，API 以 OpenAPI 为事实来源，重大决策以 `docs/adr` 为事实来源。

## 提交要求

- 每个变更聚焦一个可验证问题，保留模块边界和既有用户改动。
- 新能力应覆盖适用的成功、加载、空数据、无权限、校验失败、接口失败和并发冲突状态。
- 新依赖、中间件、数据库、独立服务或部署组件必须先用当前业务证据证明必要性；重大引入需要 ADR。
- 前端通过 `src/services` 访问数据，不得直连数据库、Broker 或绕过业务 API 修改事实。
- 新增或修改接口时同步更新 OpenAPI、迁移、测试和业务文档。
- 用户可见文案默认使用简体中文并集中管理。

提交 Pull Request 前至少运行：

```powershell
pnpm check
cd guanseq-server
.\mvnw.cmd verify
```

如果某项检查因环境原因无法执行，请在 Pull Request 中写明原因、已验证范围和剩余风险。Pull Request 应说明业务问题、实现边界、验收证据、迁移/兼容性影响，以及页面能力属于“正式后端、Mock、规划”中的哪一种。

## 许可

除非明确另行说明，向本仓库提交贡献表示你有权提供该贡献，并同意按 Apache License 2.0 授权该贡献。许可不自动授予 GuanSeq 名称或视觉标识的额外使用权。
