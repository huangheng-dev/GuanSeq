# 开发交接记录 — P0-P3 硬化

> 分支：codex/harden-p0-loop
> 最后更新：2026-08-28
> 状态：阶段 A、B 已完成；阶段 C 的核心闭环、双向反向实物流、关键移动作业、仓储上架、库内调拨与单余额盘点均已完成；阶段 D 的十二个纵向切片已完成开源工程闭环。A—D 核心正式版里程碑完成度仍为 100%；2026-08-28 已完成[整体项目进度重基线](./整体项目进度重基线-2026-08-28.md)和 `v0.1.0-alpha.1` 公开预览发行，整体项目约为 58%，尚余约 42%。R0 已闭环，R1 已完成工作区用户、只读角色权限矩阵、最小组织层级与成员归属，以及当前工作区治理审计查询。当前验证结果以仓库自动化门禁为准。

## 当前项目进度

当前定义的 **A—D 核心正式版里程碑为 100%**。这表示已承诺的业务底座、第一条制造主闭环、关键现场仓储增强和阶段 D 开源工程切片已经闭环；它不表示整个项目 100%。完整产品地图共有 236 条路径，其中 54 条接入正式后端、12 条明确规划、170 条仍是 Mock/目录能力。按[整体重基线](./整体项目进度重基线-2026-08-28.md)的五维模型，**整体项目约为 58%，尚余约 42%**。

| 阶段 | 当前状态 | 作用 | 当前成果 / 剩余边界 |
|---|---|---|---|
| A 前端产品基线 | 已完成 | 统一产品信息架构、交互与服务访问边界 | 12 个一级栏目、完整路由与统一状态体验已收口 |
| B 业务核心底座 | 已完成 | 建立可信身份、工作区、主数据、数据库迁移与 API 契约 | Spring Modulith、PostgreSQL/Flyway、OpenAPI、权限与审计基线已落地 |
| C 订单到交付与基础收付款 | 核心、双向反向实物流与关键现场作业闭环已完成 | 证明销售到交付、退货处置、收付款、利润和现场扫码能够形成真实闭环 | 第一条制造主闭环、销售/采购反向处置、三类移动扫码、受控标签、仓储上架、同仓调拨和单余额盘点均已贯通；整仓盘点、盲盘/复盘和高级策略按独立切片推进 |
| D 设备与智能制造 | 工程闭环完成（100%） | 把设备台账、维护执行、周期生成、物料消耗、成本、受控采集和 OEE 责任证据接入统一工作台 | 已完成维护、Modbus/MQTT 只读适配、样本生命周期、技术预检、现场验收状态机、报警处置和人工核实 OEE；真实接入仍由每个安装实例按验收单完成，自动 OEE 属于可选增强 |
| E AI 与高级能力 | 未进入主建设 | 在可信业务事实之上提供查询、解释、预测和建议 | 不在核心事实未完备时提前建设伪智能能力 |
| F 按规模演进 | 未启动 | 用真实容量与可靠性数据驱动拆分和高可用 | 当前继续保持模块化单体，不提前引入分布式基础设施 |

### 阶段 D 第三个切片（2026-08-25 完成）

- 建立 V39 备件台账、不可变领退事务、人工登记/冲销事务与维修成本汇总。
- 库存事实继续归属 warehouse，标准成本继续归属 finance；equipment 仅通过公开模块接口读取或提交，不越权访问其他模块表。
- `/equipment/spare-parts` 接入真实服务；维修工单详情支持领用、退回、人工登记、冲销与净成本证据查看。
- 每个写动作具备工作区权限、工单状态门禁、乐观版本、请求幂等、责任人、原因、时间与请求编号。
- 全量验证：Flyway 41 个迁移至 V39；后端 41 个测试套件、102 项测试；前端 25 个测试文件、71 项测试；生产构建和桌面/390px 浏览器闭环通过。

### 阶段 D 第四个切片（2026-08-25 完成）

- 建立 V40 周期点检/保养模板、模板事件、人工生成批次与逐项结果证据。
- 以请求编号和“模板 + 到期日”双重幂等生成真实运维工单，并保存来源模板与业务到期日。
- 模板周期参数冻结；启停使用乐观锁，生成时锁定活动模板。停用模板不新增任务，但历史逾期责任继续纳入风险指标。
- `/equipment/maintenance` 接入周期模板、活动/应生成/逾期/最近批次指标、详情、启停和人工截止日生成；明确标注独立后台调度未接入。
- 全量验证：Flyway 42 个迁移至 V40；后端 42 个测试套件、106 项测试；前端 27 个测试文件、76 项测试；生产构建和桌面/390px 浏览器闭环通过。

### 阶段 D 设备采集需求审计（2026-08-26 完成）

- 完成[设备采集需求审计](./设备采集需求审计.md)，确认当前只有设备业务台账和人工维护证据，没有真实设备连接、协议适配、点位字典、遥测存储、通讯状态或报警运行证据。
- 明确试点设备、协议与网络、点位字典、采样与容量、断连补传、质量码、报警责任和安全要求等实施准入清单。
- 建立网关、MQTT Broker、时序数据库、独立接入服务和消息组件的证据门禁；未经实测容量与 ADR，不提前引入 EMQX、TimescaleDB 或独立服务。
- 审计交付当时 `/equipment/telemetry` 保持规划状态，移除模拟设备数、点位数、在线率和运行值，改为真实现场需求提交与实施准入视图；随后已由第五个切片替换为真实服务契约。
- 本次审计本身没有新增迁移、后端 API 或采集服务，当时综合进度约 79%、阶段 D 约 60%；当前进度以文档顶部及后续第五、六个切片为准。
- 前端验证：lint、typecheck、27 个测试文件/77 项测试、生产构建，以及桌面端需求弹窗和 390px 规划页验收通过。
- 已完成仓库现场证据复核并生成[设备采集现场需求包](../outputs/guanseq-device-intake-20260826/设备采集现场需求包.xlsx)：四张表覆盖设备登记、点位字典、三场景容量公式和 18 项实施准入检查。用户授权由 Codex 编制工程验证数据后，工作簿已回填一台开发种子设备、Modbus TCP、8 个只读工程点位和三组容量场景；全部字段显式标注为工程假设，不替代真实厂商或现场证据，也不计作真实采集能力。
- 首机平均结果约 1.21 样本/秒、1.16 GB/30 天；试点峰值约 80.12 样本/秒，未来 12 个月参考约 240.23 样本/秒。工作簿公式错误扫描为 0，准入结论为 5 项通过、10 项工程假设、2 项现场阻塞、1 项不适用。
- 完成[设备采集工程验证假设与最小切片设计](./设备采集工程验证假设与最小切片设计.md)：首机继续复用模块化单体与 PostgreSQL，不引入 EMQX、TimescaleDB、独立接入服务或远程控制；随后由 ADR-0006 允许先完成生产形态代码与仿真验证，真实证据继续作为现场启用门禁。

### 阶段 D 第五个切片（2026-08-26 工程闭环完成）

- 接受 [ADR-0006](./adr/0006-无真机条件下的可替换设备采集闭环.md)，把“开始工程实现”和“真实现场启用”拆成两个门禁；仿真器与真机共享同一生产 Modbus TCP 适配器。
- 建立 V41 连接、点位、运行状态、样本和不可变连接事件；通讯状态与人工设备运行状态分离，首期只使用读取功能码。
- 增加连接创建、测试、启用、暂停、立即采集、周期轮询、当前值和审计 API，并以 OpenAPI 固化契约；前端 `/equipment/telemetry` 已接入同源 BFF 与真实服务契约。
- 增加零外部依赖的标准协议仿真端点；真实设备到位后仅需新建物理端点配置并替换点位映射，不修改核心模型和页面。
- MQTT 没有被排除：后续适配器连接用户已有的外部 Broker 并进入同一统一样本模型；当前规模不要求所有开源安装部署内置 EMQX。
- 验证结果：前端 lint、typecheck、28 个测试文件/81 项测试与生产构建通过；后端 111/111 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V41。浏览器真实完成“创建 → 测试 → 启用 → 采集 → 查看 → 暂停”，读取 `2`、`68.5%`、`true` 三个归一化点位且控制台 0 错误。以上仍不等于真实现场验收。

### 阶段 D 第六个切片（2026-08-26 工程闭环完成）

- 建立 V42 工作区级原始样本保留策略和不可变策略/清理事件；保留范围 7—3650 天，未配置时使用 30 天默认值。
- 新增默认最近 24 小时、单次最多 31 天的原始样本分页查询，可按连接、点位和质量筛选，避免页面或 API 发起无界扫描。
- `ADMIN`、`MAINTENANCE_MANAGER` 可以使用乐观版本修改策略并人工清理严格早于截止时间的样本；普通成员只读，清理使用请求编号幂等且不删除连接、点位、当前值或事件证据。
- `/equipment/telemetry` 详情增加保留摘要、有限历史筛选、策略管理和清理反馈，覆盖加载、空数据、失败、无权限、版本冲突、操作中与成功状态。
- 验证结果：前端 lint、typecheck、29 个测试文件/84 项测试与生产构建通过；后端 112/112 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V42。浏览器在真实 BFF/API/数据库链路读取 3 条样本、保存 30 天策略并执行 0 条安全清理，桌面与 390px 布局通过且控制台 0 错误/警告。
- 本切片没有自动调度、多实例租约、失败通知、趋势聚合或长期归档；这些能力不得据此宣称已完成。

### 阶段 D 第七个切片（2026-08-26 工程闭环完成）

- 建立 V43 策略自动配置、工作区租约和不可变运行证据；应用级运行开关与工作区自动策略均默认关闭，升级不会自动删除样本。
- 自动任务首次启用从下一计划时间开始；单次最多删除 10000 条，仍有过期样本时记录 `PARTIAL` 并续跑，失败采用受控摘要、产品内责任证据和指数退避。
- 多实例和人工立即运行由 PostgreSQL 工作区租约互斥；请求编号保持幂等，失败可以确认责任并在故障恢复后安全重试。
- `/equipment/telemetry` 已支持自动间隔、下次计划、立即运行、最近运行和失败确认；运行器关闭时后端拒绝启用策略，页面明确显示不可用。
- 验证结果：前端 lint、typecheck、29 个测试文件/84 项测试与生产构建通过；后端 113/113 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V43。浏览器真实完成显式启用与立即运行，桌面/390px 通过，干净会话控制台 0 错误、0 警告。
- 自动运行仅在部署方显式设置 `GUANSEQ_TELEMETRY_RETENTION_SCHEDULER_ENABLED=true` 后可用；本切片仍不代表真实设备、MQTT、现场网络、容量、报警或长期归档已完成。

### 阶段 D 第八个切片（2026-08-26 工程闭环完成）

- 接受 [ADR-0007](./adr/0007-外部MQTTBroker只读接入.md)：GuanSeq 连接部署方自行选择的兼容 Broker，不内置、不启动、不强制 EMQX，也不新增独立设备服务。
- V44 扩展统一连接/点位/样本模型，支持 MQTT 3.1.1、TCP/TLS、QoS 0/1、精确 Topic、JSON Pointer、稳定消息编号去重和可选 ISO-8601 设备时间。
- 用户名和密码仅由服务端环境变量按凭据别名解析，不写数据库、不进入 API、浏览器、日志或事件；普通成员不获得主机、Topic 和 Pointer 等敏感配置。
- 开发用最小 Broker 经过标准 CONNECT/SUBSCRIBE/PUBLISH 帧验证，但不是生产依赖；真实 Broker 只需替换端点、TLS/凭据、Topic 和 Pointer，不更换业务模型或页面。
- 验证结果：前端 lint、typecheck、29 个测试文件/85 项测试和生产构建通过；后端 117/117 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V44。浏览器真实完成“创建 → 测试 → 启用 → 采集 → 查看 → 暂停”，桌面/390px 通过，最终控制台 0 错误、0 警告。
- 本切片仍不代表用户真实 Broker、TLS/ACL、现场网络、断连恢复、容量或设备已经验收；MQTT 5 专属能力、通配/共享订阅、报警、OEE 和远程控制仍未建设。

### 阶段 D 第九个切片（2026-08-26 工程闭环完成）

- 完成[设备接入技术预检证据](./业务切片-设备接入技术预检证据.md)：继续使用生产 Modbus/MQTT 适配器和现有 `/test` API，不新增 Broker、服务、数据库或部署组件。
- 每次预检校验适配器读取、全部点位覆盖、值类型、值域和传输安全提示，并写入已有不可变连接事件；点位缺失和类型不匹配使用稳定错误码失败。
- 端点自动分为 `SIMULATION_TECHNICAL` 或 `FIELD_CANDIDATE_PRECHECK`，所有结果固定 `fieldAccepted=false`，一次连通成功不会冒充现场验收。
- 页面增加结构化检查、警告、请求编号和现场待办证据；正式接入仍只替换端点、凭据别名和点位/Topic 映射。
- 验证结果：前端 lint、typecheck、29 个测试文件/86 项测试和生产构建通过；后端 117/117 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V44。浏览器通过 MQTT 3.1.1 协议返回 3/3 点位、0 警告，桌面/390px 无横向溢出，控制台 0 错误、0 警告。
- 真实现场的 TLS/ACL、网络审批、断连恢复、容量和责任人签字仍是外部证据门禁。

### 阶段 D 第十个切片（2026-08-26 工程闭环完成）

- 完成[设备报警处置闭环 v1](./业务切片-设备报警处置闭环.md)：V45 建立报警规则、规则事件、报警实例和不可变报警事件；条件是否仍存在与人工处置状态分离。
- 支持即时上限、下限和通讯失败规则；生产采集链路负责触发、恢复与重新打开，同一规则最多保留一个未关闭责任，避免样本风暴重复建单。
- 后端状态机强制 `OPEN → ACKNOWLEDGED → IN_PROGRESS → RESOLVED → CLOSED`，只有条件恢复后才能解决；可关联同设备现有维修工单，但不自动修改设备状态、不自动建维修、不执行远程控制。
- `/equipment/alerts` 已接入真实 API/BFF，覆盖规则创建/启停、报警列表/详情、确认/处理/解决/关闭/维修关联、权限、校验失败、接口失败、提交中和并发冲突。
- 验证结果：前端 lint、typecheck、30 个测试文件/90 项测试和生产构建通过；后端 118/118 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V45，Maven 生产打包通过。
- 浏览器通过生产 Modbus TCP 适配器完成“启用仿真连接 → 创建 60% 上限规则 → 采集 68.5% → 触发 → 确认”，桌面/390px 无横向溢出，最终控制台 0 错误、0 警告。厂商质量码、迟滞/持续时间、外部通知、报警风暴和值守升级仍未建设或验收。

### 阶段 D 第十一个切片（2026-08-26 工程闭环完成）

- 完成[OEE 与停机证据闭环 v1](./业务切片-OEE与停机证据闭环.md)：V46 建立 OEE 记录、停机事件与不可变责任事件，来源明确固定为 `MANUAL_VERIFIED`，不以仿真或稀疏遥测冒充自动统计。
- 服务端统一计算运行时间、可用率、性能率、质量率与 OEE，校验统计窗口、产量、停机越界/重叠、性能率上限，以及同设备已提交/已审核时间窗口冲突。
- 状态机支持 `DRAFT → SUBMITTED → APPROVED` 与 `SUBMITTED → REJECTED → 修正后重提`；审核后口径、停机和指标全部冻结，维护与审核角色均由后端授权事实决定。
- `/equipment/oee` 已接入真实 API/BFF，覆盖建立/编辑、停机增改、提交、驳回、审核、列表筛选、指标摘要、详情与事件证据；页面持续标注“人工核实”和“现场自动来源未接入”。
- 验证结果：前端 lint、typecheck、31 个测试文件/94 项测试和生产构建通过；后端 121/121 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V46，Maven 生产打包通过。
- 浏览器真实完成“建立记录 → 登记 30 分钟停机 → 指标重算 → 提交 → 审核冻结”，生成 4 条责任事件；桌面/390px 无横向溢出，干净验收标签页控制台 0 错误、0 警告。自动生产/质量/遥测来源、OEE 与维护联动以及真实现场口径验收仍未接入。

### 阶段 D 第十二个切片（2026-08-26 工程闭环完成）

- 完成[设备现场接入验收闭环](./业务切片-设备现场接入验收闭环.md)：V47 建立现场验收单与不可变责任事件，只有 `PHYSICAL_DEVICE` 或 `EXTERNAL_BROKER` 可以进入验收，`SIMULATOR` 由后端硬拒绝。
- 状态机支持 `DRAFT → SUBMITTED → APPROVED` 与 `SUBMITTED → REJECTED → 修正后重提`；维护角色录入网络、安全、只读、断连恢复、容量和点位映射六项证据，生产经理或管理员独立审批。
- 提交和批准都会重新核对最新一次 `FIELD_CANDIDATE_PRECHECK` 成功证据；技术预检失败、证据不全、测试窗口无效、越权、重复请求与乐观并发冲突均由后端阻断。
- `/equipment/telemetry` 详情已接入真实验收上下文、草稿维护、提交/审批/驳回、责任事件和部署状态；仿真端点只显示“不适用”，无真机时允许留下 0/6 草稿但绝不生成“现场已验收”。
- 验证结果：前端 lint、typecheck、32 个测试文件/98 项测试和生产构建通过；后端 125/125 项测试通过、0 跳过，PostgreSQL 18.4 空库完整执行 V1—V47，Maven 生产打包通过。
- 浏览器用保留测试网段地址建立物理设备候选连接和 0/6 验收草稿，确认提交按钮被最新现场预检门禁锁定；仿真端点不提供验收入口，桌面/390px 无横向溢出，修复相邻动态面板重复 key 后干净验收标签页控制台 0 错误、0 警告。
- 阶段 D 至此按开源正式版标准完成工程闭环。真实设备或用户 Broker 的批准记录只能由安装现场提供真实证据后生成；自动 OEE 来源、OPC UA、边缘网关和高级报警是后续按需增强，不再作为阶段 D 完成门槛。

### 阶段 C 反向增强：销售退货与质量处置（2026-08-27 完成）

- 完成[销售退货与质量处置闭环](./业务切片-销售退货与质量处置闭环.md)：V48 建立退货授权、待检收货、质量拆分、取消/冲回与不可变责任事件，订单同时保留毛发货、累计退货和净发货口径。
- 仓库模块拥有 `INSPECTION`、`AVAILABLE`、`BLOCKED` 库存及不可变流水；销售模块不直接修改仓库或财务表，已有利润结算在履约变化后被标记为待重算，红字发票和退款仍由财务人员确认。
- `/sales/returns` 已接入真实 API/BFF，覆盖创建、收货、质量判定、取消、冲回、权限、校验失败、接口失败、提交中、幂等和并发冲突；同页授权后立即收紧剩余可退数量。
- 验证结果：前端 lint、typecheck、33 个测试文件/100 项测试和生产构建通过；后端 128/128 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V48。
- 浏览器通过真实 API/数据库完成“建立授权 → 待检收货 → 合格质量判定 → 已处置”，桌面与 390px 无横向溢出，干净会话控制台 0 错误、0 警告。

### 阶段 C 反向增强：采购退货与供应商处置（2026-08-27 完成）

- 完成[采购退货与供应商处置闭环](./业务切片-采购退货与供应商处置闭环.md)：V49 建立退货授权、原批次退回出库、取消/冲回与不可变责任事件，采购订单同时保留毛合格收货、累计合格退货和净合格收货口径。
- 仓库模块按 `AVAILABLE`/`BLOCKED` 原质量库存拥有出入库事实；只有合格库存退回影响净收货，应付模块仅标记已开票超额风险，不自动生成供应商红字或退款。
- `/procurement/returns` 已接入真实 API/BFF，覆盖建立、取消、退回出库、冲回、权限、校验失败、接口失败、提交中和并发冲突；同页授权后即时收紧可退数量。
- 验证结果：前端 lint、typecheck、34 个测试文件/102 项测试和生产构建通过；后端 131/131 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V49。
- 浏览器通过真实 API/数据库完成两条路径，并确认应付复核标记与详情风险说明；干净会话控制台 0 错误、0 警告。

### 阶段 C 现场增强：采购收货移动扫码（2026-08-27 完成）

- 完成[采购收货移动扫码闭环](./业务切片-采购收货移动扫码闭环.md)：扫码只替换输入方式，继续复用正式采购收货、来料检验和库存过账事实；支持普通键盘式扫码枪和人工输入，不依赖专用 PDA。
- `/procurement/mobile-receiving` 覆盖采购单/物料精确匹配、权限、未收数量、批号、仓库/库位、离线草稿、稳定请求编号、接口失败和正式过账；离线时不写服务端，也不后台静默同步。
- V50 固化 `DESKTOP_FORM`/`MOBILE_SCAN` 来源，V51 将采购退货后的补收约束统一为净收货口径；OpenAPI、审计事件、BFF 和前端契约保持一致。
- 验证结果：前端 lint、typecheck、36 个测试文件/106 项测试和生产构建通过；后端 133/133 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V51，Maven 生产打包通过。
- 浏览器真实生成 `PR-20260827-000003` 并在到货台账和数据库核对 `MOBILE_SCAN` 来源；刷新恢复、离线拦截、390px 无横向溢出及干净会话 0 错误/0 警告均通过。

### 阶段 C 现场增强：生产领料移动扫码（2026-08-27 完成）

- 完成[生产领料移动扫码闭环](./业务切片-生产领料移动扫码闭环.md)：扫码继续复用生产领料与仓库库存正式事实，支持领料单、组件和 `STOCK:` 精确库存标签或唯一 `LOT:` 批次，不依赖专用 PDA。
- `/production/mobile-operations/material-scan` 覆盖写权限、开放领料单、组件剩余需求、精确余额/批次、数量上限、离线草稿、稳定请求编号、接口失败、幂等和库存版本冲突；离线不写服务端，也不后台静默同步。
- V52 固化生产领料动作和库存事务的 `DESKTOP_FORM`/`MOBILE_SCAN` 来源、余额 ID 与批次证据；移动显式选批，桌面自动拣批保持兼容。
- 验证结果：前端 lint、typecheck、38 个测试文件/110 项测试和生产构建通过；后端 135/135 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V52。
- 浏览器以 390×844 完成离线拦截及恢复后正式发料；数据库确认同一请求编号仅形成一次 `MOBILE_SCAN` 动作和库存事务，控制台 0 错误、无横向溢出。

### 阶段 C 现场增强：生产扫码报工（2026-08-27 完成）

- 完成[生产扫码报工闭环](./业务切片-生产扫码报工闭环.md)：扫描只替换输入方式，继续复用正式工序任务、工序事件、生产报工与完工检验事实，不依赖专用 PDA，也不建立移动事实表。
- `/production/mobile-operations/reporting-scan` 覆盖工序任务/本人身份精确扫描、正式开工、工序完工、最后工序报工、离线草稿、稳定请求编号、错误恢复、幂等和乐观并发。
- V53 固化 `SYSTEM`/`DESKTOP_FORM`/`MOBILE_SCAN` 来源及触发报工的精确工序证据；移动人员标签必须匹配当前认证账号，桌面表单语义保持兼容。
- 验证结果：前端 lint、typecheck、40 个测试文件/115 项测试和生产构建通过；后端 136/136 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V53，Maven 生产打包通过。
- 390×844 浏览器先验证离线零写入，再完成三道工序 6 条移动事件，并生成正式报工单和待检单；数据库来源、操作人、数量和任务关联一致，控制台 0 错误、0 警告。

### 阶段 C 现场增强：受控标签生成与补打（2026-08-27 完成）

- 完成[受控标签生成与补打闭环](./业务切片-受控标签生成与补打闭环.md)：工序、本人和库存标签继续读取各自正式模块事实，labeling 只拥有不可变打印准备请求，不建立第二套主数据。
- V54 固化对象快照、Code 128B 载荷、模板版本、首次/补打模式、份数、补打原因、责任人、时间和稳定请求编号；`PREPARED` 不冒充物理出纸成功。
- `/production/mobile-operations/label-reprint` 已替换 Mock，覆盖权限、租户、本人、对象版本、首次唯一性、补打前置条件、幂等、失败恢复、浏览器打印和证据回看。
- 验证结果：前端 lint、typecheck、42 个测试文件/119 项测试和生产构建通过；后端 138/138 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V54，Maven 生产打包通过。
- 390×844 与 1440×900 浏览器完成首次生成、重复首次冲突和两份补打；数据库确认仅有两条不可变 `PREPARED` 事实，最终干净会话控制台 0 错误、0 警告。

### 阶段 C 现场增强：仓储收货上架与库内扫码（2026-08-27 完成）

- 完成[仓储收货上架与库内扫码闭环](./业务切片-仓储收货上架与库内扫码闭环.md)：warehouse 只拥有上架任务，库存余额与不可变流水仍是唯一库存事实，不复制采购收货或成品入库事实。
- V55 固化 `OPEN → COMPLETED/CANCELLED` 与 `COMPLETED → REVERSED` 状态机、同仓库库位门禁、开放任务额度占用、源/目标版本、稳定请求编号、责任人和来源证据。
- `/warehouse/receiving` 与 `/warehouse/barcodes/scanning` 已替换 Mock，支持 `STOCK:`、`LOC:` 和直接库位编码精确扫描；扫码仅替换录入方式，不依赖专用 PDA、相机 SDK、离线数据库或 Broker。
- 完成上架生成源库位 `ISSUE` 与目标库位 `RECEIPT` 两条流水，冲回只追加反向补偿流水；后端权限、事务锁、乐观版本和请求幂等共同阻止越权、超量、重复过账与陈旧写入。
- 验证结果：前端 lint、typecheck、43 个测试文件/122 项测试和生产构建通过；后端 140/140 项测试通过、0 跳过，PostgreSQL 18.4 执行 V1—V55。
- 浏览器完成 4 件库存上架，数据库核对 `COMPLETED|4.000000|8.000000|4.000000|2`，控制台 0 错误、0 警告。

### 阶段 C 仓储增强：库内调拨与盘点（2026-08-28 完成）

- 完成[库内调拨与盘点闭环](./业务切片-库内调拨与盘点闭环.md)：warehouse 只拥有任务状态和责任证据，库存余额与不可变流水仍是唯一库存事实。
- V56 固化调拨 `OPEN → COMPLETED/CANCELLED → REVERSED` 与盘点 `OPEN → COUNTED → APPROVED → REVERSED` 状态机；账面快照、开放调拨额度、固定顺序余额锁、稳定请求编号和任务乐观版本共同阻止超量、重复和陈旧过账。
- `/warehouse/inventory-operations/transfers` 与 `/warehouse/counts` 已替换 Mock，支持精确 `STOCK:` / `LOC:` 扫描、同仓调拨、实盘录入、差异审批、取消和补偿冲回；离线状态坚持零写入。
- 验证结果：前端 lint、typecheck、44 个测试文件/124 项测试和生产构建通过；后端 143/143 项测试、0 跳过，新增 3 个库存控制集成场景和 13 项仓储/MRP 相关回归通过，PostgreSQL 18.4 执行 V1—V56，Maven 生产打包通过。
- 1440×900 浏览器完成 5 件调拨和 1 件盘亏审批；数据库核对 `COMPLETED|5|MOV-1049|MOV-1050`、`APPROVED|493|492|-1|ISSUE|MOV-1051` 及 3 条移动事实，控制台 0 错误、0 警告。

### R0 正式页面契约稳定性第一项（2026-08-28 完成）

- 根因是 V48 已将销售订单合法状态扩展为 `PARTIALLY_RETURNED`、`RETURNED`，并在订单行增加累计退货和净发货口径，但 OpenAPI、前端 Zod 契约与销售订单状态文案未同步；数据库一旦存在退货订单，正式销售订单页会整体解析失败。
- 已统一后端响应事实、OpenAPI 与前端契约：销售订单支持两种退货状态，订单行公开 `deliveredQuantity`、`returnedQuantity`、`netDeliveredQuantity`，应收引用数据接受发货后发生退货的订单状态。
- 已增加前端销售订单/应收契约回归和 OpenAPI 精确枚举断言，避免以后只修改后端状态机而遗漏消费者契约。
- 完整验证：前端 lint、typecheck、44 个测试文件/128 项测试和 56 个路由生产构建通过；后端 143/143 项测试、0 跳过，58 个迁移在 PostgreSQL 18.4 验证通过。
- 浏览器在真实 BFF/API/数据库链路确认销售订单页正确显示 `SO-20260827-000001` 为“已全部退货”，应收页正常加载，两页控制台均 0 错误。本项修复没有增加正式路径数量，因此完整项目综合进度仍约 49%。

### R0 开源治理与能力真实性第二项（2026-08-28 完成）

- 新增 Apache License 2.0、NOTICE、贡献指南、安全策略、支持策略、社区行为准则、更新日志、版本/发布策略，以及结构化 Issue 和 Pull Request 模板。
- 明确 `0.1.0` 是开发快照，`0.x` 不承诺长期兼容；只有更新日志明确标记的稳定版本才进入支持窗口。“正式后端”只表示页面事实来源，不代表整套系统或工厂实例生产就绪。
- 新建统一成熟度模型：正式页面注册表优先，其次识别已规划模块，其余为 Mock。236 条路径唯一分类为 50 条正式后端、174 条 Mock、12 条规划，并在左侧导航和所有页面首屏显示。
- 增加路径全覆盖、分类数量、典型路径和治理文件边界测试；前端 46 个测试文件、132 项测试全绿，lint、类型检查与生产构建通过。
- 浏览器通过本地 development 身份、真实 BFF/API 和 PostgreSQL 链路分别验收 `/sales/orders/list`、`/sales/orders/approvals`、`/settings/integrations/logs`；三页首屏依次显示“正式后端、Mock 原型、规划能力”，1440px 导航无标识遮挡，控制台均 0 错误、0 警告。
- 本项把“开源发行与生产运维”维度从 30% 提升到 40%，综合项目进度从约 49% 更新为约 50%；正式路径仍是 50 条，未因标签增加而虚增业务完成度。

### R0 可重复生产部署与恢复第三项（2026-08-28 完成）

- 接受 [ADR-0008](./adr/0008-最小生产容器部署与外部TLS边界.md)，正式运行拓扑保持“外部 HTTPS 入口 → Web → 模块化单体 → PostgreSQL”，未引入 Redis、RabbitMQ、EMQX、MinIO、Kubernetes 或额外代理依赖。
- 新增前后端多阶段 OCI 镜像和 `compose.production.yaml`；两个应用以非 root、只读根文件系统、删除全部 capabilities 与 `no-new-privileges` 运行，数据库和后端不暴露宿主端口，Web 默认只绑定回环地址。
- 新增生产配置 fail-closed 预检和真实 `/api/health`：Web readiness 必须同时确认后端 `/api/v1/platform/status`，示例占位密钥、非明确版本、错误 OIDC/TLS 回调和不安全数据库标识会阻止部署。
- 新增 PostgreSQL custom-format 备份、SHA-256/版本/Flyway 清单和随机隔离库恢复演练；本次真实生成 587774 字节备份，恢复后核对 56 条成功迁移，演练库自动清理且原库未被覆盖。
- 新增源码/依赖/密钥/配置 Trivy 门禁、双镜像构建、双 SPDX JSON SBOM 和双镜像 HIGH/CRITICAL 阻断工作流；配置及合同测试已通过，但 GitHub 托管首跑仍需由后续推送触发，不能记为已经执行。
- 实际构建 `guanseq/web:0.1.0-rc.1` 和 `guanseq/server:0.1.0-rc.1`，生产 Compose 三服务均 Healthy；健康响应同时返回 Web/Server `UP` 与同一版本，只有 `127.0.0.1:3300` 对宿主开放。
- 完整验证：前端 lint、typecheck、49 个测试文件/140 项测试和生产构建通过；后端 Maven verify 的 143/143 项测试通过、0 跳过。生产部署与恢复工程基线闭环，但真实 TLS/OIDC、异机备份、集中可观测和业务验收仍属于具体安装实例责任。
- 本项把“开源发行与生产运维”维度从 40% 提升到 65%，综合项目进度更新为约 54%；正式业务路径仍为 50 条，没有借部署能力虚增页面真实化率。

### R1 系统管理/IAM 第一项（2026-08-28 完成）

- 将已经具备数据库、OpenAPI、后端授权、审计和前端服务接入的 `/settings/organization/users` 纳入正式页面注册表与统一失败关闭门禁，修复其被误标为 Mock 的真实性缺陷。
- 浏览器发行冒烟加入工作区成员和系统管理员断言；正式路径由 50 增至 51，Mock 由 174 降至 173，没有改变业务实现本身或虚增综合进度。

### R1 系统管理/IAM 第二项（2026-08-28 完成）

- 完成[只读角色—权限矩阵](./业务切片-只读角色权限矩阵.md)：`identity` 输出 13 个受控内置角色、52 个后端显式角色门禁和 11 个业务分组，仅当前工作区 `ADMIN` 可读。
- 48 个业务应用服务的实际角色集合由自动化测试与目录逐项比对；身份治理和会计期间专门规则另行锁定，目录漂移会阻断测试。
- `/settings/roles` 已替换原 Mock 新建/勾选授权交互，支持角色、业务分组和动作筛选、刷新、失败重试与请求编号；没有自定义角色、组合权限、字段权限或 IdP 角色同步写入口。
- 正式路径由 51 增至 52，Mock 由 173 降至 172；系统管理真实化率由 3.0% 提升至 6.1%，综合项目仍约 57%。
- 完整验证：前端 lint、typecheck、50 个测试文件/142 项测试和 57 个路由生产构建通过；后端 Maven verify 的 145/145 项测试通过、0 跳过，58 个迁移在 PostgreSQL 18.4 空库验证通过；生产镜像 Playwright 浏览器 E2E 1/1 通过并自动清理临时环境。

### R1 系统管理/IAM 第三项（2026-08-28 完成）

- 完成[最小组织层级与成员归属](./业务切片-最小组织层级与成员归属.md)：公司根只读，当前工作区管理员可治理所在工厂、直属现场、当前工作区负责人和成员主要归属，不获得跨工作区权限。
- Flyway V57 增加组织/工作区负责人和乐观锁版本，并为成员关系增加非空组织归属；既有成员从工作区所在组织回填。
- 新增正式 OpenAPI、后端授权、占用停用保护、原因审计、同源 BFF 和专用组织工作台；负责人/归属不自动产生角色权限。
- `IDENTITY_ORGANIZATION_MANAGE` 进入权限目录，显式权限由 52 增至 53；正式路径由 52 增至 53，Mock 由 172 降至 171，系统管理真实化率由 6.1% 提升至 9.1%，综合项目约为 58%。
- 完整验证：前端 lint、typecheck、51 个测试文件/145 项测试和 58 个路由生产构建通过；后端 Maven `verify` 的 146/146 项测试通过、0 跳过，59 个 Flyway 迁移（V1—V57 与 2 个本地可重复种子）在 PostgreSQL 18.4 空库执行，生产打包通过；生产镜像 Playwright 浏览器 E2E 1/1 通过并自动清理临时环境。托管 CI 由本轮提交推送后核验。

### R1 系统管理/IAM 第四项（2026-08-28 完成）

- 完成[当前工作区治理审计查询](./业务切片-当前工作区治理审计查询.md)：`/settings/audit` 从 Mock 切换为正式 BFF/API，只读取当前工作区 `identity.audit_events`。
- 新增 `IDENTITY_AUDIT_READ`，仅 `ADMIN` 可读；默认最近 30 天、单次最长 90 天，支持事件、对象、操作人、请求/对象编号筛选和稳定服务端分页。
- V58 为既有审计表增加工作区/时间/类型/对象/请求编号索引；没有新建日志服务、消息中间件或跨模块直查。
- 查询本身不写审计，防止递归噪声；销售、采购、生产、质量、库存和财务责任链继续由对应业务单据持有。
- 正式路径由 53 增至 54，Mock 由 171 降至 170；系统管理真实化率由 9.1% 提升至 12.1%，综合项目仍约为 58%。
- 完整验证：前端 lint、typecheck、52 个测试文件/147 项测试和 59 个路由生产构建通过；后端 Maven `verify` 的 148/148 项测试通过、0 失败、0 错误、0 跳过，60 个 Flyway 迁移（V1—V58 与 2 个本地可重复种子）在 PostgreSQL 18.4 执行成功；生产镜像 Playwright 发行冒烟加入组织事件审计详情和请求编号筛选，E2E 1/1 通过并自动清理临时环境。

## 已完成切片

### 1. 预收预付（Advance Receipts & Payments）

- 后端：V32 迁移 + AdvanceController/ApplicationService/Entities/Repositories。
- 应收/应付开票支持 advanceId 自动抵扣。
- 前端：/finance/advances 工作台、开票弹窗预收/预付抵扣下拉。
- 集成测试：FinanceAdvanceIntegrationTest（4 用例）。
- 文档：docs/业务切片-预收预付.md（已存在，状态"已实现"）。
- 本轮补修：测试加 EntityManager.flush() 解决 @Transactional 下 JdbcTemplate 查不到事件的问题。

### 2. 供应商主数据（Supplier Master Data）

- 后端：SupplierController/ApplicationService/Record/Page，复用 V13 suppliers 表。
- V33 新增 supplier_events 审计表。
- V34 新增 version 乐观锁列。
- 角色：PROCUREMENT_MANAGER / ADMIN 可写；其他角色只读，写返回 403。
- 停用供应商不能建新采购单（已有 requireSupplier ACTIVE 校验，返回 422）。
- 前端：/procurement/suppliers/list 接入 BusinessWorkspace，导航加"供应商列表"子菜单。
- 集成测试：ProcurementSupplierIntegrationTest（6 用例：CRUD+审计、重复编码、低权限 403、跨租户 404、版本冲突 409、停用供应商建单 422）。
- 文档：docs/业务切片-供应商主数据.md（本轮新建）。

### 3. OpenAPI 契约硬化

- 修复 guanseq-api-v1.yaml 中 description 含未转义冒号导致 YAML 解析失败。
- 删除重复的 WarehouseOption schema key。
- 补全 Advance 和 Supplier 的 paths/schemas。
- OpenApiContractTest 改为真实 YAML 解析（SnakeYAML），并断言新契约。
- Supplier schema 含 version；UpdateSupplierRequest/SupplierStatusRequest 含 expectedVersion。

### 4. 前端真实/Mock 边界

- 17 个 *-server-service.ts：后端返回非 OK 时抛出明确错误，不再静默回退 mock。
- 网络无响应（response=null）才返回 null/unavailable。
- receivable/payable credit note 列表：非 OK 抛错，网络错误才返回空数组。
- master-data-server-service：供应商接入真实后端；supplier 路径纳入 isBackendMasterDataPath。
- 开票成功后异步刷新 advances 列表（fetchAdvances → setAdvances）。

### 5. MRP 日期敏感测试修复

- PlanningMrpRunIntegrationTest.freezesSupply... 加 @Transactional。
- 测试开始时把 DMD-260815-001 的 required_date 动态更新为当天，避免日期漂移。
- 注入 EntityManager 并在快照断言前 flush。

## 检查命令

前端：
```
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

后端（Maven 装在 C:\tools\apache-maven-3.9.9，JDK 25）：
```powershell
$env:JAVA_HOME = "C:\Users\Administrator\.jdks\temurin-25.0.3_9\jdk-25.0.3+9"
$env:PATH = "C:\tools\apache-maven-3.9.9\bin;$env:PATH"
cd guanseq-server
mvn test
```

## 下一步主线

1. **R0 正式开源发行基线与能力真实性治理**：正式页面契约稳定性、开源治理文件/版本边界、236 条路径成熟度三态、生产部署/恢复、提交式浏览器 E2E 和公开预览发行均已完成。
2. **R1 已完成前四项**：工作区用户管理、管理员只读后端角色权限矩阵、最小组织层级与成员归属、当前工作区治理审计查询已经闭环；不把工作区治理审计宣称为跨模块日志平台。
3. **当前下一项——质量不合格评审与 CAPA 最小闭环**：先固化不合格来源、评审、处置、责任措施、验证和关闭证据，再设计数据库/API/页面。
4. **下一批高价值业务切片**：生产追溯与异常、仓储销售出库、真实经营工作台和分析继续按纵向闭环替换 Mock，不以页面数作为完成标准。
5. **安装实例现场验收与按需增强**：试点方提供真实设备或已有 MQTT Broker 证据后复用 V47 验收；自动 OEE、OPC UA、可选网关、AI 和阶段 F 仍按真实需求、容量和责任证据立项。
