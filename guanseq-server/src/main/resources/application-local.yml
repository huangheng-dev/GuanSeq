spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/local

guanseq:
  security:
    mode: ${GUANSEQ_SECURITY_MODE:development}
    development-username: ${GUANSEQ_DEV_USERNAME:lin.hao}
    development-password: ${GUANSEQ_DEV_PASSWORD:guanseq_dev}
